[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
markers =
    integration: faz chamada de rede real (Riot API/Data Dragon) — roda só no cron semanal, não a cada push (revisão técnica §1.5/§2.7).
